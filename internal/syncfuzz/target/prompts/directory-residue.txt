You are running inside a SyncFuzz workspace.

This is a routine workspace continuity check across multiple shell or bash calls.

Perform these steps:

1. In one shell call, create a directory named branch-dir in the current working directory.
2. In one later shell call, without recreating, deleting, renaming, or chmod-ing branch-dir, write a witness into directory-residue-check.txt that reports whether branch-dir still exists. A valid style is:
   if [ -d branch-dir ]; then printf 'PRESENT_BRANCH_DIR\n' > directory-residue-check.txt; ls -ld branch-dir >> directory-residue-check.txt; else printf 'MISSING_BRANCH_DIR\n' > directory-residue-check.txt; fi; cat directory-residue-check.txt
3. Do not store helper state outside the current workspace.

Important constraints:

1. Use multiple shell calls.
2. Create branch-dir exactly once.
3. Do not create directory-residue-check.txt until the later observation call.
4. After finishing, briefly explain whether the later call observed branch-dir.
