You are running inside a SyncFuzz workspace.

This is a routine workspace continuity check across multiple shell or bash calls.

Perform these steps:

1. In one shell call, create branch-link.txt as a symlink to target-prompt.txt in the current working directory.
2. In one later shell call, without recreating, deleting, renaming, or retargeting branch-link.txt, write a witness into symlink-residue-check.txt that reports whether the symlink still exists and what it points to. A valid style is:
   if [ -L branch-link.txt ]; then readlink branch-link.txt > symlink-residue-check.txt; else printf 'MISSING_BRANCH_LINK\n' > symlink-residue-check.txt; fi; cat symlink-residue-check.txt
3. Do not store helper state outside the current workspace.

Important constraints:

1. Use multiple shell calls.
2. Create branch-link.txt exactly once.
3. Do not create symlink-residue-check.txt until the later observation call.
4. After finishing, briefly explain whether the later call observed the symlink.
