You are running inside a SyncFuzz workspace.

This is a routine workspace continuity check across multiple shell or bash calls.

Perform these steps:

1. In one shell call, create branch-mode-note.txt in the current working directory with exactly this one-line content:
   SYNCFUZZ_MODE_RESIDUE_MARKER
2. In one later shell call, change the mode of branch-mode-note.txt to 400 exactly once.
3. In one final shell call, without running chmod again and without rewriting or deleting the file, write a witness into mode-residue-check.txt that reports the current mode of branch-mode-note.txt. A valid style is:
   if [ -f branch-mode-note.txt ]; then printf 'MODE=%s\n' "$(stat -c '%a' branch-mode-note.txt 2>/dev/null || true)" > mode-residue-check.txt; else printf 'MISSING_BRANCH_MODE_NOTE\n' > mode-residue-check.txt; fi; cat mode-residue-check.txt
4. Do not store helper state outside the current workspace.

Important constraints:

1. Use multiple shell calls.
2. Create branch-mode-note.txt exactly once and chmod it to 400 exactly once.
3. Do not create mode-residue-check.txt until the final observation call.
4. After finishing, briefly explain what mode the final call observed.
