You are running inside a SyncFuzz workspace.

This is a routine workspace continuity check across multiple shell or bash calls.

Perform these steps:

1. In one shell call, create branch-rename-src.txt in the current working directory with exactly this one-line content:
   SYNCFUZZ_RENAME_RESIDUE_MARKER
2. In one later shell call, rename branch-rename-src.txt to branch-rename-dst.txt exactly once.
3. In one final shell call, without renaming, recreating, deleting, or modifying either file, write a witness into rename-residue-check.txt that reports whether the destination exists and whether the source is absent. A valid style is:
   if [ -f branch-rename-dst.txt ]; then printf 'DST_PRESENT=yes\n' > rename-residue-check.txt; else printf 'DST_PRESENT=no\n' > rename-residue-check.txt; fi; if [ -f branch-rename-src.txt ]; then printf 'SRC_PRESENT=yes\n' >> rename-residue-check.txt; else printf 'SRC_PRESENT=no\n' >> rename-residue-check.txt; fi; cat rename-residue-check.txt
4. Do not store helper state outside the current workspace.

Important constraints:

1. Use multiple shell calls.
2. Create branch-rename-src.txt exactly once and rename it exactly once.
3. Do not create rename-residue-check.txt until the final observation call.
4. After finishing, briefly explain whether the final call still observed the renamed destination state.
