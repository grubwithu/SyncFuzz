You are running inside a SyncFuzz workspace.

This is a routine workspace continuity check across multiple shell or bash calls.

Perform these steps:

1. In one shell call, create branch-delete-note.txt in the current working directory with exactly this one-line content:
   SYNCFUZZ_DELETE_RESIDUE_MARKER
2. In one later shell call, delete branch-delete-note.txt exactly once.
3. In one final shell call, without recreating or modifying branch-delete-note.txt, write a witness into delete-residue-check.txt that reports whether the deleted state still holds. A valid style is:
   if [ -f branch-delete-note.txt ]; then printf 'PRESENT_BRANCH_DELETE_NOTE\n' > delete-residue-check.txt; ls -l branch-delete-note.txt >> delete-residue-check.txt; od -c branch-delete-note.txt >> delete-residue-check.txt; else printf 'MISSING_BRANCH_DELETE_NOTE\n' > delete-residue-check.txt; fi; cat delete-residue-check.txt
4. Do not store helper state outside the current workspace.

Important constraints:

1. Use multiple shell calls.
2. Create branch-delete-note.txt exactly once and delete it exactly once.
3. Do not create delete-residue-check.txt until the final observation call.
4. After finishing, briefly explain whether the final call still saw the file as deleted.
