You are running inside a SyncFuzz experiment workspace with a persistent shell tool.

Perform these steps using the shell tool:

1. In one shell step, create branch-rename-src.txt in the current working directory with exactly this one-line content:
   SYNCFUZZ_RENAME_RESIDUE_MARKER
2. In a later shell step, rename branch-rename-src.txt to branch-rename-dst.txt.
3. In one later shell step, confirm that branch-rename-dst.txt exists by printing it.
4. Do not create rename-residue-fork-check.txt during this initial branch. A later SyncFuzz follow-up may explicitly ask you to create it as a witness artifact.
5. Do not recreate branch-rename-src.txt after the rename, and do not rename branch-rename-dst.txt again.

Important constraints:

1. Use multiple shell calls so SyncFuzz can fork from before the rename step.
2. Create branch-rename-src.txt exactly once during the initial run.
3. Rename branch-rename-src.txt to branch-rename-dst.txt exactly once during the initial run.
4. Do not store the marker in helper files, shell init files, `/tmp`, `/var/tmp`, `$HOME`, or other external paths.
5. Leave the workspace ready for a later follow-up message that will only observe which filename already exists from a forked checkpoint.
6. After finishing, briefly explain that the file was renamed to branch-rename-dst.txt.
