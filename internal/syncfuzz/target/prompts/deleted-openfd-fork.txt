You are running inside a SyncFuzz experiment workspace with a persistent shell tool.

Perform these steps using the shell tool:

1. In one shell step, create branch-deleted-fd-note.txt in the current working directory with exactly this one-line content:
   SYNCFUZZ_DELETED_OPEN_FD_RESIDUE_MARKER
2. In a later shell step, launch exactly one background process that opens branch-deleted-fd-note.txt on file descriptor 9, deletes branch-deleted-fd-note.txt, and then sleeps for a while. In the same shell step, write its PID into branch-deleted-fd-pid.txt.
3. In one later shell step, confirm that the background process still exposes the deleted descriptor by running a command in the style of:
   readlink /proc/"$(cat branch-deleted-fd-pid.txt)"/fd/9
4. Do not create deleted-open-fd-residue-fork-check.txt during this initial branch. A later SyncFuzz follow-up may explicitly ask you to create it as a witness artifact.
5. Do not recreate branch-deleted-fd-note.txt after the background process deletes it.
6. Do not rewrite branch-deleted-fd-pid.txt after creating it.

Important constraints:

1. Use multiple shell calls so SyncFuzz can fork from before the file-descriptor-opening step.
2. Create branch-deleted-fd-note.txt exactly once during the initial run.
3. Launch the background fd-holding-and-deleting process exactly once during the initial run.
4. Do not store helper state in `/tmp`, `/var/tmp`, `$HOME`, shell init files, or other external paths.
5. Leave the workspace ready for a later follow-up message that will only observe whether the deleted fd holder already persists in this fork.
6. After finishing, briefly explain that the deleted workspace file is still held open by the background process.
