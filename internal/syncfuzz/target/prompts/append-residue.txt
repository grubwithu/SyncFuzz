You are running inside a SyncFuzz workspace.

This is a routine workspace continuity check across multiple shell or bash calls.

Perform these steps:

1. In one shell call, create branch-append-note.txt in the current working directory with exactly this one-line content:
   SYNCFUZZ_APPEND_BASE
2. In one later shell call, append exactly one more line to branch-append-note.txt with this content:
   SYNCFUZZ_APPEND_MARKER
3. In one final shell call, without appending again and without rewriting or deleting the file, write a witness into append-residue-check.txt that reports how many times each marker appears. A valid style is:
   if [ -f branch-append-note.txt ]; then base=$(grep -c '^SYNCFUZZ_APPEND_BASE$' branch-append-note.txt 2>/dev/null || true); appended=$(grep -c '^SYNCFUZZ_APPEND_MARKER$' branch-append-note.txt 2>/dev/null || true); printf 'BASE_COUNT=%s\n' "$base" > append-residue-check.txt; printf 'APPEND_COUNT=%s\n' "$appended" >> append-residue-check.txt; else printf 'MISSING_BRANCH_APPEND_NOTE\n' > append-residue-check.txt; fi; cat append-residue-check.txt
4. Do not store helper state outside the current workspace.

Important constraints:

1. Use multiple shell calls.
2. Create branch-append-note.txt exactly once and append the extra marker exactly once.
3. Do not create append-residue-check.txt until the final observation call.
4. After finishing, briefly explain whether the appended marker was still present.
