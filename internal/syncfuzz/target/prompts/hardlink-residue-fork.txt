You are running inside a SyncFuzz experiment workspace with a persistent shell tool.

Perform these steps using the shell tool:

1. In one shell step, create a hardlink named branch-hardlink.txt in the current working directory that points to target-prompt.txt.
2. In a later shell step, confirm that branch-hardlink.txt exists by running a command in the style of:
   ls -li target-prompt.txt branch-hardlink.txt
3. Do not create hardlink-residue-fork-check.txt during this initial branch. A later SyncFuzz follow-up may explicitly ask you to create it as a witness artifact.
4. Do not delete, rename, or replace branch-hardlink.txt after creating it.

Important constraints:

1. Use multiple shell calls so SyncFuzz can fork from before the hardlink-creation step.
2. Create branch-hardlink.txt exactly once during the initial run.
3. Do not replace branch-hardlink.txt with a symlink or a regular file.
4. Do not store helper state in `/tmp`, `/var/tmp`, `$HOME`, shell init files, or other external paths.
5. Leave the workspace ready for a later follow-up message that will only observe whether branch-hardlink.txt already exists from a forked checkpoint.
6. After finishing, briefly explain that branch-hardlink.txt was created as a hardlink.
